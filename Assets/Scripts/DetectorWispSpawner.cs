using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DetectorWispSpawner : MonoBehaviour
{
    // Update is called once per frame
    public float detectRadius = 4f;
    void Update()
    {
        //get all enemies
        //check their distances
        //spawn a wisp for each enemy if their distance less than distance
    }
}
